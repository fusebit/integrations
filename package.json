{
  "name": "@fusebit-int/packages",
  "private": true,
  "devDependencies": {
    "husky": "^7.0.0",
    "lerna": "^4.0.0",
    "prettier": "2.4.0"
  },
  "dependencies": {},
  "scripts": {
    "prepare": "husky install",
    "packages:publish": "./scripts/prepare_publish.sh",
    "packages:version": "./scripts/version.sh",
    "prettier:fix": "prettier --write .",
    "prettier:check": "prettier --check ."
  }
}
