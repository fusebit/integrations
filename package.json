{
  "name": "@fusebit-int/packages",
  "private": true,
  "devDependencies": {
    "husky": "^7.0.0",
    "lerna": "^4.0.0",
    "prettier": "2.4.0"
  },
  "dependencies": {},
  "scripts": {
    "prepare": "husky install",
    "packages:publish": "./scripts/prepare_publish.sh",
    "packages:version": "./scripts/version.sh",
    "prettier:fix": "git ls-tree -r `git branch --show-current` --name-only | grep -E \"\\.[tj]?sx?$|\\.yaml$|\\.json$\" | grep -v assets | grep -v lerna.json | xargs -P 1 prettier --write",
    "prettier:check": "git ls-tree -r `git branch --show-current` --name-only | grep -E \"\\.[tj]?sx?$|\\.yaml$|\\.json$\" | grep -v assets | grep -v lerna.json | xargs -P 1 prettier --check"
  }
}
