{
  "type": "object",
  "properties": {
    "scope": {
      "title": "Scope",
      "description": "Space separated scopes to request from your HubSpot app",
      "type": "string"
    },
    "tokenUrl": {
      "title": "HubSpot Token URL",
      "description": "The authentication URL to use when exchanging tokens, as per the OAuth protocol",
      "type": "string"
    },
    "authorizationUrl": {
      "title": "HubSpot Authorization URL",
      "description": "The authorization URL to use when generating authorization codes, as per the OAuth 2.0 protocol",
      "type": "string"
    },
    "clientId": {
      "title": "Client ID",
      "description": "The Client ID from your HubSpot app",
      "type": "string"
    },
    "clientSecret": {
      "title": "Client Secret",
      "type": "string",
      "description": "The Client Secret from your HubSpot app"
    },
    "webhookUrl": {
      "title": "Configure the HubSpot App with this webhook URL",
      "type": "string",
      "description": "Webhook Url",
      "readOnly": true
    },
    "redirectUrl": {
      "title": "Configure the HubSpot App with this redirect URL",
      "type": "string",
      "description": "Redirect Url",
      "readOnly": true
    },
    "refreshErrorLimit": {
      "type": "integer"
    },
    "refreshInitialBackoff": {
      "type": "integer"
    },
    "refreshWaitCountLimit": {
      "type": "integer"
    },
    "refreshBackoffIncrement": {
      "type": "integer"
    },
    "accessTokenExpirationBuffer": {
      "type": "integer"
    }
  },
  "required": ["scope", "clientId", "clientSecret"]
}
