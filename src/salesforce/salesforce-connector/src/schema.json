{
  "type": "object",
  "properties": {
    "scope": {
      "title": "OAuth Scopes",
      "description": "Space separated scopes to request from your Salesforce Connected App",
      "type": "string"
    },
    "tokenUrl": {
      "title": "OAuth Token URL",
      "description": "The authentication URL to use when exchanging tokens, as per the OAuth protocol",
      "type": "string"
    },
    "authorizationUrl": {
      "title": "OAuth Authorization URL",
      "description": "The authorization URL to use when generating authorization codes, as per the OAuth protocol",
      "type": "string"
    },
    "clientId": {
      "title": "OAuth Consumer key",
      "description": "The OAuth Consumer Key from your Salesforce Connected App",
      "type": "string"
    },
    "clientSecret": {
      "title": "OAuth Consumer Secret",
      "type": "string",
      "description": "The Consumer Secret from your Salesforce Connected App"
    },
    "refreshErrorLimit": {
      "type": "integer"
    },
    "refreshInitialBackoff": {
      "type": "integer"
    },
    "refreshWaitCountLimit": {
      "type": "integer"
    },
    "refreshBackoffIncrement": {
      "type": "integer"
    },
    "accessTokenExpirationBuffer": {
      "type": "integer"
    }
  },
  "required": ["scope", "clientId", "clientSecret"]
}
