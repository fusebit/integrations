export enum EntityType {
  integration = 'integration',
  connector = 'connector',
  operation = 'operation',
  storage = 'storage',
  install = 'install',
  identity = 'identity',
  session = 'session',
}
