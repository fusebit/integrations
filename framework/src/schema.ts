export enum EntityType {
  integration = 'integration',
  connector = 'connector',
  operation = 'operation',
  storage = 'storage',
  instance = 'instance',
  identity = 'identity',
  session = 'session',
}
