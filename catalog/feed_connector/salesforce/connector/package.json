{
  "name": "@fusebit-int/catalog-feed-connector-salesforce-connector",
  "version": "6.0.0",
  "private": true,
  "scripts": {
    "redeploy": "fuse connector rm <%this.name%> && yarn deploy",
    "deploy": "fuse connector deploy <%this.name%> -d .",
    "get": "fuse connector get <%this.name%> -d ."
  },
  "dependencies": {
    "@fusebit-int/framework": "^6.0.0",
    "@fusebit-int/salesforce-connector": "^6.0.0"
  }
}
