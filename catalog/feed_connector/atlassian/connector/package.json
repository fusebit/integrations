{
  "name": "@fusebit-int/catalog-feed-connector-atlassian-connector",
  "version": "7.10.0",
  "private": true,
  "scripts": {
    "redeploy": "fuse connector rm <%this.name%> && yarn deploy",
    "deploy": "fuse connector deploy <%this.name%> -d .",
    "get": "fuse connector get <%this.name%> -d ."
  },
  "dependencies": {
    "@fusebit-int/atlassian-connector": "^7.10.0",
    "@fusebit-int/framework": "^7.10.0"
  }
}
