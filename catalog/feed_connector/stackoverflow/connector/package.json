{
  "name": "@fusebit-int/catalog-feed-connector-stackoverflow-connector",
  "version": "7.47.15",
  "private": true,
  "scripts": {
    "redeploy": "fuse connector rm <% this.id %> && yarn deploy",
    "deploy": "fuse connector deploy <% this.id %> -d .",
    "get": "fuse connector get <% this.id %> -d ."
  },
  "dependencies": {
    "@fusebit-int/framework": "^7.47.15",
    "@fusebit-int/stackoverflow-connector": "^7.47.15"
  }
}
