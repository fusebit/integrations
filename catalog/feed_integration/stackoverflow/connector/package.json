{
  "name": "@fusebit-int/catalog-feed-integration-stackoverflow-connector",
  "version": "7.45.7",
  "private": true,
  "scripts": {
    "redeploy": "fuse connector rm <% this.id %> && yarn deploy",
    "deploy": "fuse connector deploy <% this.id %> -d .",
    "get": "fuse connector get <% this.id %> -d ."
  },
  "dependencies": {
    "@fusebit-int/framework": "^7.45.7",
    "@fusebit-int/stackoverflow-connector": "^7.45.7"
  }
}
